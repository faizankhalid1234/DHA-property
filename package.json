{
  "name": "dha-housing-scheme",
  "version": "1.0.0",
  "description": "DHA Housing Scheme Management System",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "predev": "npx --yes kill-port 5000",
    "dev": "concurrently -n backend,frontend,admin -c blue,green,magenta \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "seed": "npm run seed --prefix backend",
    "test:sale": "node backend/scripts/test-sale-flow.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
